Make a unit test for "Backend.Accounts.User" that does a gql query to the "register_with_password" mutation, then tests the "sign_in_with_password", then makes the call to the "read_one(:me, :me)" gql query. Place the unit test in the standard Elixir test dir idiom. Do not mock any code. We want this postgres tests to run agaist a database named whatever our current ecto test db. Set the test db up by running the same ecto migration. Run the gql parts of the test by sending hard_coded gql mutations for "Register", "signInWithPassword", and the query to "read_one(:me, :me)" to a test instance of Absinthe. We want all code paths to be exercised in this one unit test.
